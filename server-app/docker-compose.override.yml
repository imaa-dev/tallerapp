version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: mybike-mysql-dev
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mybike_dev
      MYSQL_USER: dev
      MYSQL_PASSWORD: dev
    volumes:
      - mysql-dev-data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password

volumes:
  mysql-dev-data:
